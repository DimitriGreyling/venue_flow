enum FieldType {
  text,
  dropdown,
  checkbox,
  textarea,
  radio,
  date,
}

enum FormStatus {
  active,
  inactive,
  draft,
}

enum UserRole {
  client,
  coordinator,
  admin,
}

enum EventStatus{
  booked,
  awaitingClient,
  inprogress,
  cancelled,
}